import React from "react";
import Select from "@/components/Form/Item/Select";

const RangeSelect = ({children, className, ...restProps}) => {
    return (
        <Select className="w-36" />
    );
};

export default RangeSelect;